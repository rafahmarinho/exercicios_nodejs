// ## Importe a biblioteca ##
let rs = require('readline-sync')

// ## Faça o código ##
let texto = rs.question("Digite um texto qualquer para contarmos os caracteres.")
console.log("Seu texto possui " + texto.length + " caracteres.") 